services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: intelligen
      POSTGRES_PASSWORD: intelligen
      POSTGRES_DB: intelligen_lms
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
